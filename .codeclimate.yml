engines:
  eslint:
    enabled: true
    channel: "eslint-2"
    config:
      config: '.eslint.codeclimate.json'
  duplication:
    enabled: true
    config:
      languages:
        - javascript
ratings:
   paths:
    - "**.js"
exclude_paths:
  - "test/"
  - "**.json"
